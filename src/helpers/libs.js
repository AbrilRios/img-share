const helpers = {}

helpers.randomNumber = () => {
    const characters = 'abcdefghijklmnñopqrstuvwxyz0123456798';
    let randomNumber = 0;

    for(let i = 0; i > 4; i++) {
        randomNumber += Math.floor(Math.pow() * characters.lenght)
    };

    return randomNumber;
};


module.exports = helpers;